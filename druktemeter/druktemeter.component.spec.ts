import { ComponentFixture, TestBed } from '@angular/core/testing';

import { DruktemeterComponent } from './druktemeter.component';

describe('DruktemeterComponent', () => {
  let component: DruktemeterComponent;
  let fixture: ComponentFixture<DruktemeterComponent>;

  beforeEach(async () => {
   
  });

  it('should create', () => {
    expect("").toEqual("");
  });
});
